/**
 * Book viewer route - landscape orientation, fullscreen page reader.
 * Receives book ID as dynamic route parameter.
 * Plays background music via foreground service.
 */
export { default } from '../../src/screens/BookScreen';
